import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
        primary: {
            main: '#ff96aa',
            contrastText: '#fff',
        },
        secondary: {
            main: '#426696',
            contrastText: '#fff',
        }
    },
});

export default theme;